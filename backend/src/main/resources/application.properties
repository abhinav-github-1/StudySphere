# Server Configuration
server.port=${TOMCAT_PORT:8085}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}


# Spring Data MongoDB Configuration
# Uses MONGODB_URI env var (perfect for MongoDB Atlas) or falls back to local localhost MongoDB connection
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://StudySphere:abhi%40123@cluster0.ysygd98.mongodb.net/studyroom?retryWrites=true&w=majority&appName=Cluster0}

# Spring Security CORS and Logging Configs
logging.level.org.springframework.security=INFO

# Application Custom Security Configurations
app.security.jwt.secret=${JWT_SECRET:407f34079813247d56e792b02e7df687d6e409c91d8e137f8f9e6d420a8e0f6c}
app.security.jwt.expiration-ms=86400000

# Socket.IO Server Configuration (Netty — runs alongside REST on separate port)
socketio.port=9092
socketio.host=0.0.0.0
